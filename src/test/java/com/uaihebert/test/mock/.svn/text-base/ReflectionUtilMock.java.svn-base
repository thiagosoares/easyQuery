package com.uaihebert.test.mock;

import com.uaihebert.util.ReflectionUtil;

public class ReflectionUtilMock extends ReflectionUtil {

    public static boolean isJoinAttributeInteger(String canonicalName, String attributeName) {
        try {
            throw new ClassNotFoundException(canonicalName + attributeName);
        } catch (ClassNotFoundException e) {
            System.err.println("Something really bad happened: " + e.getMessage());
            e.printStackTrace();
        }

        return false;
    }

    public static boolean isJoinAttributeLong(String canonicalName, String attributeName) {
        try {
            throw new ClassNotFoundException(canonicalName + attributeName);
        } catch (ClassNotFoundException e) {
            System.err.println("Something really bad happened: " + e.getMessage());
            e.printStackTrace();
        }

        return false;
    }

    public static boolean isJoinAttributeDouble(String canonicalName, String attributeName) {
        try {
            throw new ClassNotFoundException(canonicalName + attributeName);
        } catch (ClassNotFoundException e) {
            System.err.println("Something really bad happened: " + e.getMessage());
            e.printStackTrace();
        }

        return false;
    }

    public static boolean isJoinAttributeFloat(String canonicalName, String attributeName) {
        try {
            throw new ClassNotFoundException(canonicalName + attributeName);
        } catch (ClassNotFoundException e) {
            System.err.println("Something really bad happened: " + e.getMessage());
            e.printStackTrace();
        }

        return false;
    }

    public static boolean isJoinAttributeDate(String canonicalName, String attributeName) {
        try {
            throw new ClassNotFoundException(canonicalName + attributeName);
        } catch (ClassNotFoundException e) {
            System.err.println("Something really bad happened: " + e.getMessage());
            e.printStackTrace();
        }

        return false;
    }

    public static boolean isJoinAttributeCalendar(String canonicalName, String attributeName) {
        try {
            throw new ClassNotFoundException(canonicalName + attributeName);
        } catch (ClassNotFoundException e) {
            System.err.println("Something really bad happened: " + e.getMessage());
            e.printStackTrace();
        }

        return false;
    }

    public static boolean isJoinAttributeString(String canonicalName, String attributeName) {
        try {
            throw new ClassNotFoundException(canonicalName + attributeName);
        } catch (ClassNotFoundException e) {
            System.err.println("Something really bad happened: " + e.getMessage());
            e.printStackTrace();
        }

        return false;
    }

    public static boolean isJoinAttributeBigDecimal(String canonicalName, String attributeName) {
        try {
            throw new ClassNotFoundException(canonicalName + attributeName);
        } catch (ClassNotFoundException e) {
            System.err.println("Something really bad happened: " + e.getMessage());
            e.printStackTrace();
        }

        return false;
    }
}